import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function HTMLQuotationsPage() {
  return (
    <div className="space-y-12">
      <section className="text-center">
        <h1 className="text-4xl font-extrabold tracking-tight lg:text-5xl mb-4">HTML Quotations</h1>
        <p className="text-xl text-muted-foreground max-w-2xl mx-auto">
          Learn how to use HTML to create quotations and citations in your web pages.
        </p>
      </section>
    </div>
  )
} 